<tr>
  <th>Name</th>
  <th>Color</th>
  <th>Expansion</th>
  <th>Deck</th>
  <th>Need</th>
  <th colspan="2">Action</th>
</tr>
<% picks.each do |pick| %>
  <tr id="pick-<%= pick.id %>">
    <%= card_columns(pick.card) %>
    <td><%= pick.total %></td>
    <td><%= pick.diff %></td>
    <td><%= link_to_remote 'Edit', :url => edit_pick_url(pick), :method => :get %></td>
    <td><%= link_to 'Delete', pick_url(pick), :method => :delete %></td>
  </tr>
<% end %>