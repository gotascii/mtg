<td colspan="7" id="pick-<%= pick.id %>" class="form-container">
  <% remote_form_for pick, :loading => "$('#pick-#{pick.id}').toggleClass('loading')", :html => {:class => "inline"} do |f| %>
    <%= render "form", :f => f, :pick => pick, :colors => colors, :search => search %>
  <% end %>
</td>